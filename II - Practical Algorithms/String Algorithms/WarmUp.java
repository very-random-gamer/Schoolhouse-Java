public class WarmUp {
    public static void main(String[] args) {
        int min = 5;
        int max = 10;

        // write code that returns the square of a random integer between min and max (inclusive)
        /* reminders:
            (int) (Math.random() * (max - min + 1) + 1) -- Returns random integer
            Math.pow(int a, int b) -- returns double value of a^b
         */
    }
}