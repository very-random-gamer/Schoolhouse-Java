public class hypotenuse {
    public static void main(String[] args) {
        int x = 5;
        int y = 12;

        // write a formula that will print out the hypotenuse
        double hypotenuse = Math.sqrt(Math.pow(x, 2) + Math.pow(y, 2));
        System.out.println(hypotenuse);
    }
}